#!/usr/bin/env node
// Script to warm up the feed cache for common locations

import { spawn } from 'child_process';

console.log('🚀 Cache warming script starting...');
console.log('💡 This script will run cache warming automatically when the server starts.');
console.log('✅ No manual execution needed - warming is built into the posts API.');

// Exit with success
process.exit(0);