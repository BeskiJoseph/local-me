rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 🔒 RESTRICTIVE POLICY: No direct client-side access allowed.
    // All data operations must go through the authorized backend API.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
