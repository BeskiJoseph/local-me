import 'package:flutter/material.dart';

/// @deprecated Use [AppTheme] from `config/app_theme.dart` instead.
/// All color constants have been merged into AppTheme.
class AppColors {
  static const primaryBlue = Color(0xFF2563EB);
  static const primaryText = Color(0xFF111827);
  static const secondaryText = Color(0xFF6B7280);
  static const mutedText = Color(0xFF9CA3AF);

  static const appBackground = Color(0xFFFFFFFF);
  static const cardBackground = Color(0xFFFFFFFF);
  static const dividerBorder = Color(0xFFE5E7EB);

  static const activeIcon = Color(0xFF2563EB);
  static const inactiveIcon = Color(0xFF9CA3AF);

  static const likeActive = Color(0xFFEF4444);
}
